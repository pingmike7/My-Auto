name: Auto Renew Teoheberg

on:
  workflow_dispatch:
  schedule:
    - cron: '0 18 * * *' # 每天 UTC 18 点运行

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      # =========================
      # 📥 Checkout 主仓库
      # =========================
      - name: Checkout main repo
        uses: actions/checkout@v4

      # =========================
      # ⭐ Checkout 私有仓库
      # =========================
      - name: Checkout private repo
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository_owner }}/teoheberg-renew
          token: ${{ secrets.PRIVATE_REPO_TOKEN }}
          path: scripts

      # =========================
      # 🟢 Node.js
      # =========================
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # =========================
      # 🐍 Python（语音识别）
      # =========================
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # =========================
      # 📦 安装 Node + Python 依赖
      # =========================
      - name: Install dependencies
        run: |
          npm init -y
          npm install playwright axios
          sudo apt-get update
          sudo apt-get install -y xvfb ffmpeg curl p7zip-full pulseaudio
          pip install SpeechRecognition pydub

      # =========================
      # 🌐 安装 Chromium 浏览器
      # =========================
      - name: Install Chromium
        run: npx playwright install chromium

      # =========================
      # 🔊 虚拟音频环境
      # =========================
      - name: Setup audio
        run: |
          pulseaudio --start || true
          pactl load-module module-null-sink sink_name=virtual_speaker || true
          pactl set-default-sink virtual_speaker || true

      # =========================
      # 🌐 可选 WARP
      # =========================
      - name: WARP
        uses: fscarmen/warp-on-actions@v1.3
        with:
          stack: dual
          mode: client

      # =========================
      # 🚀 运行主脚本
      # =========================
      - name: Run script
        env:
          TEOHEBERG_REMEMBER_WEB_COOKIE: ${{ secrets.TEOHEBERG_REMEMBER_WEB_COOKIE }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        run: |
          xvfb-run --auto-servernum --server-args="-screen 0 1920x1080x24" node scripts/main.js